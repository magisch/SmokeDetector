language: python
sudo: false
python:
  - '2.7'
install:
  - pip wheel phonenumbers -w $HOME/wheelhouse --find-links $HOME/wheelhouse
  - pip install phonenumbers --use-wheel --find-links $HOME/wheelhouse
  - pip install beautifulsoup4 requests websocket-client coverage pytest
script:
  - python -W default::Warning -m pytest test
branches:
  only:
    - master
    - travis
cache:
  directories:
    - $HOME/.cache/pip
    - $HOME/wheelhouse
before_cache:
  - rm -f $HOME/.cache/pip/log/debug.log
